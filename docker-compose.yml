version: '3.8'

services:
  android-controller:
    build: .
    ports:
      - "8080:8080"
    environment:
      - NODE_ENV=production
      - DEVICE_ID=localhost:5555
      - PORT=8080
    network_mode: host
    restart: unless-stopped
    volumes:
      - ./logs:/app/logs
